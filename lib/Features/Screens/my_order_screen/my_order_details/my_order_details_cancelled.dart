import 'package:flutter/material.dart';

class MyOrderDetailsCancelled extends StatelessWidget {
  const MyOrderDetailsCancelled({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Order Cancelled")),
    );
  }
}