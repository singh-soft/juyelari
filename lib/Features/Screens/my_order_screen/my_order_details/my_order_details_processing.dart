import 'package:flutter/material.dart';

class MyOrderDetailsProcessing extends StatelessWidget {
  const MyOrderDetailsProcessing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Order Processing"),
    );
  }
}